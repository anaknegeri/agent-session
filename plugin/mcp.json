{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "agent-session": {
      "command": "./bin/agent-session-mcp",
      "cwd": "${PLUGIN_ROOT}",
      "type": "stdio"
    }
  }
}